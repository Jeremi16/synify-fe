# Spotify Clone Frontend

## Local Dev
- `npm run dev` for normal API usage
- `npm run dev:mock` to run against local mock data (no backend required)

## Environment
- `NEXT_PUBLIC_API_URL` to point at your backend
- `NEXT_PUBLIC_USE_MOCK_API=true` to force mock API responses
